---
title: 第 2 关 · 你的第一个小工具
level: 2
duration: 30 分钟
language: zh
author: BuilderX Club
created: 2026-08-11
tags: [造物挑战, 工具]
---

# 第 2 关 · 你的第一个小工具

> 30 分钟，做一个解决你生活里**真实小麻烦**的工具。
> 前面两关是"玩"，这一关是"用"。做完这一关，你就是一个真正的 Builder 了。

---

## 先听一个故事

阿泽是中文系研究生，最烦的是每周要交"读书笔记"，每次都憋不出来。

他让 AI 做了个"读书笔记生成器"：把书名、作者、他读到的三个关键词输进去，AI 就生成一篇结构完整的读书笔记初稿，他再改改就能交。

室友看到都惊呆了："这玩意儿能借我用吗？"

阿泽说："这是我做的。"

**从这一关开始，你不只是"用 AI"，你是"造工具给别人用"。**

## 你要造什么

一个解决**你自己真实问题**的小工具。做完之后，它长这样：

- 打开页面
- 输入几个信息
- 点按钮
- 得到结果

比如：
- 📝 **读书笔记生成器**：输入书名+关键词 → 生成笔记初稿
- 🍽️ **晚餐决策器**：输入预算+人数 → 生成一周菜单
- 💰 **零钱记账器**：输入收支 → 自动算结余
- 🎁 **礼物推荐器**：输入对方特点 → 推荐礼物

**选一个你真正需要的**，别选"听起来很酷但我用不上"的。

## 动手（30 分钟）

### 第 1 步：想清楚你要造什么（3 分钟）

先问自己三个问题，写在纸上或备忘录里：

1. **我生活里有什么重复的小麻烦？**（比如：每周不知道吃什么）
2. **这个麻烦的输入是什么？**（比如：预算、人数、忌口）
3. **我希望输出什么？**（比如：一周菜单）

> 这就是产品经理的第一步——不是写代码，是**想清楚"给谁解决什么问题"**。

### 第 2 步：告诉 AI 你的想法（5 分钟）

回到 Google AI Studio（https://aistudio.google.com），新建一个对话，输入：

```
帮我做一个【晚餐决策器】网页工具，只有一个 index.html 文件。
功能：
1. 页面顶部有标题"今天吃什么"
2. 用户输入：预算（元）、人数（人）、忌口（可选填）
3. 点击"帮我决定"按钮后，随机生成一周 7 天的晚餐建议
4. 每条建议包含：菜名、大概费用、简单做法描述
5. 风格：可爱一点，暖色，按钮大
6. 全部中文
7. 输出完整的 HTML 代码
```

把【晚餐决策器】换成你自己的工具，把功能描述改成你第 1 步想清楚的输入/输出。

> **关键技巧：** 结尾一定要加"**输出完整的 HTML 代码**"，这样 AI 会把能做出来的整段代码给你。

### 第 3 步：让 AI 做出来（10 分钟）

AI 会输出完整代码。复制它，然后用上一关的方法打开：

- 在线预览：https://htmlpreview.github.io/ 或 https://codepen.io/pen/
- 或者保存为 `index.html` 双击打开

**测试它：**
- 输入预算 500、人数 3、忌口"不吃辣" → 点按钮
- 看它生成的菜单对不对

**发现问题就说（回到 AI Studio）：**

```
菜单里有辣的，我说了忌口不吃辣，请把辣的全去掉，重新输出完整代码。
```

### 第 4 步：让它变得好用（5 分钟）

观察你自己用的时候哪里别扭，然后指挥它改：

```
按钮点完之后，结果区域滚动到最下面，方便直接看到结果。重新输出完整代码。
```

或者：

```
增加一个"换一批"按钮，不刷新页面就能重新生成。重新输出完整代码。
```

每次改完，重新复制新代码 → 刷新预览 → 测试。

### 第 5 步：分享出去（7 分钟）

现在你有个真家伙了。让别人也能用：

**最简单的方式**：把 `index.html` 文件发给朋友，他们双击就能打开用。

**更好一点**：部署到网上（免费），得到一个链接，谁都能访问。

部署（推荐）：
- **Vercel**（免费）：vercel.com → 用 GitHub 或邮箱注册 → New Project → 上传你的 index.html → Deploy → 得到一个网址
- 或者用 **Netlify Drop**（更简单）：https://app.netlify.com/drop → 直接把 `index.html` 拖进去 → 得到网址

> 部署如果卡住了，先别纠结——**先让 3 个朋友用上，比部署到网上更重要。**

## 你的作品

一个"输入 → 点按钮 → 出结果"的工具。

找个朋友试试，看他用的时候会不会卡住，然后告诉 AI 改进。

## 分享你的作品

截图你的工具页面 + 你朋友用它时的反应，发到社区：

> "我做了个【晚餐决策器】，再也不用纠结吃什么了！谁要链接？"

**让别人用上你的工具，是你从"学着玩"到"真正 Builder"的分水岭。**

完成后，进入下一关：做出你的第一个 AI 机器人。

## 这一关你学会了什么

- **工具 = 输入 + 处理 + 输出**。想清楚这三样，你就能"造"任何东西
- 不用从零造——**AI 写代码，你做产品经理**
- 让真实用户用，比什么都重要

---

*下一关：[第 3 关 · 你的第一个 AI 机器人](../03-first-bot/)*

---

> 🎉 **恭喜！你已经完成了热身三关，手里有了三个真实作品。**
> 接下来你可以继续闯关，也可以来参加我们的线下 **BuilderX Builder Day** 创作日——和一群和你一样的人，现场把想法做得更大。
> 👉 关注社区公告 / Discord 获取活动信息
